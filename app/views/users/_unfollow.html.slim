- relationship = current_user.active_relationships.find_by(followed_id: @user.id)

= form_with model: relationship, method: :delete, class: 'd-grid' do |f|
  = f.submit 'Unfollow', class: 'btn btn-primary'
